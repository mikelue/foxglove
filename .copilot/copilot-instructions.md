## for pom.xml

* The properties naming convention for versions of dependencies and plugins are `${version.<name>}` and `${version.plugin.name}`.
* The versions of dependencies and plugins should be defined in the `<properties>` section.
